<div id="message_<%= message.id %>" class="chat-bubble-row chat-bubble-row--assistant">
  <div class="chat-bubble chat-bubble--error">
    <strong><%= title.presence || "Error" %></strong>
    <div><%= error_message %></div>
  </div>
</div>
