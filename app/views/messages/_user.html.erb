<% user ||= local_assigns[:message] %>
<div id="message_<%= user.id %>" class="chat-bubble-row chat-bubble-row--user">
  <div class="chat-bubble chat-bubble--user">
    <div id="message_<%= user.id %>_content"><%= user.content %></div>
  </div>
</div>
