<%= link_to fallback_path,
            class: "text-decoration-none text-muted small fw-semibold",
            data: { controller: "history-back", action: "click->history-back#goBack" } do %>
  <i class="fa-solid fa-arrow-left me-1"></i><%= local_assigns.fetch(:label, "Back") %>
<% end %>
