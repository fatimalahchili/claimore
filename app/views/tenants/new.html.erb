<h1>Add a tenant</h1>

<%= form_with model: @tenant, url: property_tenants_path(@property) do |f| %>
  <%= f.collection_select :user_id, User.all, :id, :email %>
  <%= f.select :status, Tenant.statuses.keys %>
  <%= f.submit %>
<% end %>
